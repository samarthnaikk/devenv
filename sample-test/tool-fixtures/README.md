# Tool Fixture Workspace

This folder provides a stable sandbox target for `core.tools` tests and manual demos.

- `app/` contains Python application code.
- `docs/` contains markdown documentation.
- `data/` contains configuration data.
- `notes/` contains project planning notes.
